package com.nexblocks.authguard.api.dto.validation.violations;

public enum ViolationType {
    EXCEEDS_VALUE_BOUNDARIES,
    EXCEEDS_LENGTH_BOUNDARIES,
    MISSING_REQUIRED_VALUE,
    INVALID_VALUE,
    EMPTY_LIST
}
